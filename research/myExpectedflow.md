my expected output for this workflow is

BEGIN - webhook once jirra ticket is created -> agent1 ( u name it as per the task) understands the task reads the description meta data imp data sees titcket prorotiy range ->agent 2(database agent) adds data to the temp memory for later use case -> agent 3 understand total usecase plans a detailed flow of steps to resolve this prblm -> at each step db or temp db is being saved with the previo state next state and all so that no breakdage happens total agents have shared memory -> exex agents( controals sub agents like bash, sandbox, coder, planner, superviser) -> based on the plan each agent gests allocated with a taask -> heavy lifting heavy work takes place carefully -> once the fixes are done testing agent creates testcases to the modified code part does testing inside the sandbox -> if not passed or still erros -> loops back to the coder agent to fix it -> not a endless loop has limits -> once code caseas passes -> final checker agent sese whether the iseu is resolved code is correct testscases are passed or not -> if yes then does run final lint syntax other isseu tests are done -> raise new pr with porper description and information ->finnaly msgs in slack or mails to user(which ever is free prefer slack max) telling change done prblm sol wt how y along with pr link tells user to check pr and change the ticket status- END
